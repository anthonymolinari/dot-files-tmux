# split panes using V and H
bind H split-window -h
bind V split-window -v
unbind '"'
unbind %

# pane selection
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# copy mode navigation (vim keys)
setw -g mode-keys vi

# mouse mode
set -g mouse on

# more config
set -g base-index 1
setw -g pane-base-index 1

#**************#
### THEMEING ###
#**************#

set -g @themepack 'basic'

# status bar
# set -g status-right '#{prefix_highlight}| #{tmux_mode_indicator} | #{battery_status-bg} Batt: #{battery_icon} #{batter_percentage} #{battery_remain} | %a %h-%d %H:%M '

#*************#
### PLUGINS ###
#*************#

### TPM ### 
set -g @plugin 'tmux-plugins/tpm'

### Superhero Theme ###
#set -g @plugin 'leighmccullock/tmux-colours-superhero'

### tmux-sensible ### 
set -g @plugin 'tmux-plugins/tmux-sensible'

### tmux fuzzy finder ###
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'jaclu/tmux-menus'
set -g @plugin 'rickstaa/tmux-notify'
set -g @plugin 'ofirgall/tmux-window-name'
set -g @plugin 'joshmedeski/tmux-nerd-font-window-name'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'ofirgall/tmux-window-name'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'MunifTanjim/tmux-mode-indicator'
set -g @plugin 'jimeh/tmux-themepack'

# load package manager
run '~/.config/tmux/plugins/tpm/tpm'
